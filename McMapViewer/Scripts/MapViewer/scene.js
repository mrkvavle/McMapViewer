/// <reference path="/js/libraries/three.js" />
function resetScene() {
	//window.mapViewer.camera.position.y = 800;

	//scene.fog = new THREE.Fog(0xffffff, 1, 5000);
	//scene.fog.color.setHSL(0.6, 0, 1);

	//dpr = 2;
	//if (window.mapViewer.devicePixelRatio !== undefined) {
	//	dpr = window.mapViewer.devicePixelRatio;
	//}

	//renderScene = new THREE.RenderPass(scene, window.mapViewer.camera);
	//effectFXAA = new THREE.ShaderPass(THREE.FXAAShader);
	//effectFXAA.uniforms['resolution'].value.set(1 / (window.mapViewer.innerWidth * dpr), 1 / (window.mapViewer.innerHeight * dpr));
	//effectFXAA.renderToScreen = true;

	//composer = new THREE.EffectComposer(renderer);
	//composer.setSize(window.mapViewer.innerWidth * dpr, window.mapViewer.innerHeight * dpr);
	//composer.addPass(renderScene);
	//composer.addPass(effectFXAA);
}

// creates Object3D's to which the region meshes are added
